__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.coverage
dist/
.superpowers/
.claude/
.worktrees/
.ruff_cache/
.pyrefly_cache/

# Retrieval eval: the question set is committed (it is the instrument, and
# runs are only comparable against a fixed one), the per-run scores are not.
eval-results.json
